-- auto-generated definition

CREATE TABLE motoclube.users
(
    id       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name     VARCHAR(255),
    email    VARCHAR(255),
    password VARCHAR(255),
    active   BOOLEAN,
    CONSTRAINT pk_users PRIMARY KEY (id)
);

